<div>
<a href="{{ post.url }}" class="flex mb-8"><img src="{{ post.data.featuredImage }}" alt="{{ post.data.imageAlt }}" width="100%" height="auto"></a>
<h3 class="mt-0 mb-2"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
<div class="mb-24">by <span>{{ post.data.author }}</span> on <span>{{ post.date | postDate }}</span></div>
<p class="mb-0">{%- if (post.data.excerpt) -%}{{ post.data.excerpt }}{%- else -%}{{ post.templateContent | excerpt }}{%- endif -%}<a href="{{ post.url }}" class="read-more under--clr"> Read more...</a></p>
</div>
